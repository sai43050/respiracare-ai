# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environment
venv/
env/
.env

# Datasets & large zips (never push to GitHub)
*.zip
dataset/
*.csv

# Large model weights — Railway will use TorchXRayVision auto-download
# Keep only the custom-trained audio & xray weights we built (<50MB each)
# audio_model_weights.pth -> OK (44MB, within limit)
# model_weights_densenet121.pth -> OK (28MB)
# Exclude the redundant copies:
audio_model_weights_resnet18.pth
model_weights.pth
model_weights_resnet18.pth

# DB — Railway uses Postgres, not SQLite
lung_disease.db

# Uploads folder (runtime only)
uploads/*
!uploads/.gitkeep

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
